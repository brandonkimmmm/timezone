overwrite: true
schema: "./src/graphql/schemas/**/*.graphql"
documents: null
generates:
  ./src/graphql/types/index.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      scalars:
        Date: Date
