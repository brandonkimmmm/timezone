overwrite: true
schema: "./src/graphql/schemas/**/*.gql"
documents: null
generates:
  ./src/graphql/types/index.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      scalars:
        Date: Date
